import is_even from "@imsaif/is_even";
import is_obj from "is-obj";
import {a} from "../utility.js"
console.log(`JS: is Obj {a:1} ${is_obj({a:1})}`);
console.log(`JS: is Obj 123 ${is_obj(123)}`);
console.log(`JS: is Even 4 ${is_even(4)}`); // true
console.log(`JS: is Even 5 ${is_even(5)}`); // false

console.log(`JS: a from utility.js ${a}`);